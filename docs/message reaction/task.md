# Implement Message Reaction in Chat

- [ ] Investigate existing Message and Reaction models.
- [ ] Update domain models (`Message`, `MessageDto`).
- [ ] Add SQLDelight migration / Supabase migration for `message_reactions` table.
- [ ] Update Data Layer (Repositories and DataSources).
- [ ] Update Domain Layer (UseCases for reacting and removing reaction).
- [ ] Update UI (`ChatScreen`, `MessageBubble`) to display and add reactions.
